# 2026-04-05 工作日志

## 今日目标

今天的重点不是继续扩单点能力，而是把已经成型的 bench 工具链、无硬件 stub 演练链、真机准备链和数据沉淀链补成更完整的工程闭环。

围绕这个目标，今天主要推进了五件事：

1. 把 `bench-pack` 证据链补成可归组、可对比、可渲染、可汇总的 bench 记录系统
2. 把无硬件 stub 场景沉淀成固定样例、离线回归和自动测试
3. 把本地检查、GitHub Actions 和协作文档补齐到可持续维护状态
4. 把真机回工位前的准备、首跑、收尾流程做成命令化工具链
5. 把一次真实或模拟 bench 的结果接入 candidate、review、promote、merge plan 的数据沉淀闭环

---

## 一、工具执行模式与真机模板链补齐

今天先把工具层的执行模式控制做得更明确，避免后面回工位时对当前运行环境状态判断不清。

完成的内容包括：

- 新增 `ieda tools mode`
- 新增 `ieda tools setup-stub`
- 新增 `ieda tools use-real`
- 在工具执行结果里补充更适合现场排查的字段：
  - `transport_state`
  - `axis_health`
  - `observations`
  - `next_action`

对应提交：

- `8e2e510 Add explicit tool execution mode controls`

随后继续把真机联调模板链补齐，避免回工位后出现“知道要测，但不知道怎么记、怎么留痕、怎么复盘”的问题。

新增文档：

- [real_bench_readiness_checklist.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\docs\real_bench_readiness_checklist.md)
- [real_bench_first_run_template.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\docs\real_bench_first_run_template.md)
- [real_bench_issue_capture_template.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\docs\real_bench_issue_capture_template.md)
- [real_bench_session_review_template.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\docs\real_bench_session_review_template.md)

分别覆盖：

- 开工前检查
- 首次只读采证记录
- 异常留痕与风险判断
- 单次 bench session 结束后的复盘

对应提交：

- `afb7872 Add real bench readiness checklist`
- `f0092f6 Add real bench first-run record template`
- `5d4fc4d Add real bench issue capture template`
- `83addad Add real bench session review template`

---

## 二、bench-pack 证据链补成完整工作流

今天把 bench 证据从“原始命令输出”继续推进成“结构化、可归档、可复盘、可比较”的工作流。

首先落地了 `bench-pack`：

- `ieda tools bench-pack "..."`

它会自动采集：

- `tools mode`
- `tools doctor`
- tool plan
- execution
- parsed structured output

默认输出目录：

- `reports/bench_packs/`

在这个基础上，今天又继续补齐了整条证据链：

- `render-pack`
- `render-session`
- `sessions`
- `compare-pack`
- `compare-pack --session-id ...`
- `render-session` 自动带最近两包差异摘要

后续又补了 bench session 的归组与索引能力：

- `--session-id`
- `--label`
- session bundle 汇总
- session index Markdown 首页

对应提交：

- `d81c737 Add bench-pack evidence bundle command`
- `250bd24 Render bench-pack into markdown drafts`
- `0ad3de5 Add session review rendering for bench packs`
- `d3fc171 Add bench-pack session grouping`
- `dcc11c0 Add bench-pack session bundle rendering`
- `59472ac Add bench session index views`
- `98d9f00 Add bench-pack comparison and daily worklog`
- `cca53b2 Add multi-scenario WSL stub modes`
- `d9f4993 Add session-aware compare-pack shortcut`
- `f706c4c Include latest compare summary in session bundles`

今天这条链的工程意义是：

- bench 结果不再只是临时输出
- 同一轮 bench 的多个只读包可以自然归组
- 比较结果可以自动进入复盘文档
- 一次 session 的证据、差异、总结可以统一归档

---

## 三、无硬件 stub 场景沉淀为离线样例与回归资产

由于今天仍然不在 bench 旁边，手边没有真实伺服驱动器和电机，所以继续围绕无硬件 stub 演练推进。

新增或增强的 stub 场景包括：

- `nominal`
- `encoder_stall`
- `axis1_fault`
- `open_rpmsg_fail`

然后把这些场景正式沉淀成项目资产：

- [stub_scenario_cases_v1.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\materials\stub_scenario_cases_v1.md)
- [material_index_v1.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\materials\material_index_v1.md)
- [benchmark_v1.jsonl](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\benchmark\benchmark_v1.jsonl)
- [data/examples/stub_bench_packs](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs)

固定离线样例包括：

- [sample_nominal.json](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\sample_nominal.json)
- [sample_encoder_stall.json](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\sample_encoder_stall.json)
- [sample_axis1_fault.json](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\sample_axis1_fault.json)
- [sample_open_rpmsg_fail.json](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\sample_open_rpmsg_fail.json)
- [sample_compare_matrix.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\sample_compare_matrix.md)
- [offline_regression_guide.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\examples\stub_bench_packs\offline_regression_guide.md)

对应提交：

- `697d1da Add stub scenario benchmark and case seeds`
- `bab5c94 Add offline stub bench-pack sample set`
- `b9b5572 Add stub sample compare matrix`
- `11eefa0 Add offline stub regression guide`

工程意义是：

- 无硬件演练不再只是调试时临时用一下
- 现在已经变成可复用的样例集、benchmark 种子和 case 资产
- 后续新人或未来自己都可以直接拿固定样例做稳定回归

---

## 四、把离线链推成自动化测试、统一检查与 CI

今天继续把这条 bench 证据链推进到自动化回归层。

新增测试：

- [test_bench_pack_render.py](D:\桌面\Industrial%20Embedded%20Dev%20Agent\tests\test_bench_pack_render.py)

目前已覆盖的关键能力包括：

- compare-pack 的差异识别
- session bundle 的聚合
- render-pack 的 Markdown 渲染
- render-session 的最新变化摘要
- finish、review、promote、merge plan 这条后续链路

同时落地了统一回归入口：

- `ieda check`
- `ieda check --include-rag --rag-type tool_safety`
- `ieda check --include-offline`

本地检查现在会覆盖：

- `pytest`
- `rules benchmark`
- `tool_safety benchmark`
- 可选 `rag(tool_safety)`
- 可选离线 stub 样例回归

此外今天还把这套检查接入 GitHub Actions，并拆成了双层 CI：

- [quick-check.yml](D:\桌面\Industrial%20Embedded%20Dev%20Agent\.github\workflows\quick-check.yml)
- [full-check.yml](D:\桌面\Industrial%20Embedded%20Dev%20Agent\.github\workflows\full-check.yml)

同时更新了：

- [README.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\README.md)
- [CONTRIBUTING.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\CONTRIBUTING.md)

对应提交：

- `379ca06 Add offline bench-pack regression tests`
- `cedc6d9 Add unified local check command`
- `fc42b5a Add optional RAG checks to local validation`
- `8e4051d Add offline sample regression checks`
- `e565632 Add GitHub Actions CI workflow`
- `0190224 Document local checks and CI workflow`
- `4f263b3 Split CI into quick and full checks`

今天这部分的意义是：

- 这套 bench 证据链已经不是“只能手工试”
- 而是有本地检查、有固定样例、有自动测试、有 CI 兜底

---

## 五、真机准备包、kickoff 与一键渲染链落地

为了让回工位后的首轮只读 bench 尽量做到开箱即用，今天把真机准备包做成了正式命令。

新增命令：

- `ieda tools prep-real-bench --session-id ... --label ...`

自动生成内容包括：

- `00_index.md`
- `01_readiness_checklist.md`
- `02_first_run_record.md`
- `03_issue_capture.md`
- `04_session_review.md`
- `doctor_snapshot.json`
- `plan_seed.json`

并且继续增强了准备包上下文：

- 自动写入当前 branch 和 commit
- 自动写入 `tools doctor` 关键快照
- 自动固化首个只读请求 seed
- 自动生成更像 README 的 index 首页说明

对应提交：

- `f2b26c2 Add real bench prep pack generator`
- `28c694e Enrich real bench prep with runtime context`
- `1ffc2af Add doctor snapshot to real bench prep`
- `593f5c4 Add plan seed to real bench prep`
- `7ff48bc Improve real bench prep index guidance`

随后继续把从准备包进入首轮只读采证的第一步串起来：

- `ieda tools kickoff-real-bench "<plan_seed.json>"`

并逐步增强为：

- `--render-first-run`
- `--render-session-review`
- `--render-all`

现在这条链已经可以做到：

- 读取准备包中的 `plan_seed.json`
- 生成首份 seeded `bench-pack`
- 自动渲染 `first-run`
- 自动渲染 `session-review`
- 自动归档 kickoff 产物
- 自动生成 kickoff 层级的 summary

对应提交：

- `300561b Add real bench kickoff command`
- `12c6069 Add first-run rendering to real bench kickoff`
- `940de25 Add session review rendering to real bench kickoff`
- `15533a4 Add render-all shortcut to real bench kickoff`
- `ddbec74 Archive kickoff outputs into prep bundles`

这部分的工程意义是：

- 回工位后的第一轮 bench 不再需要手工拼命令
- 从准备包到第一份只读证据包已经非常接近一条命令起步

---

## 六、finish-real-bench 与数据沉淀闭环补齐

今天后半段最关键的推进，是把一次首轮 bench session 的收尾与数据沉淀真正做成闭环。

首先新增：

- `ieda tools finish-real-bench --session-id <id>`

它会在准备包目录下生成：

- `finish_outputs/session_bundle.md`
- `finish_outputs/kickoff_run_summary.json`
- `finish_outputs/kickoff_run_summary.md`
- `finish_outputs/final_summary.json`
- `finish_outputs/final_summary.md`

并且会导出三类候选数据草稿：

- `finish_outputs/candidate_exports/case_candidate.md`
- `finish_outputs/candidate_exports/log_candidate.json`
- `finish_outputs/candidate_exports/benchmark_candidate.json`

对应提交：

- `ad1c771 Add finish-real-bench closing pack command`
- `32819bb Export finish-pack candidate dataset drafts`

接着继续补了人工审核入口：

- `ieda tools review-finish-candidates --session-id <id>`

输出：

- `finish_outputs/candidate_review/review_summary.json`
- `finish_outputs/candidate_review/review_summary.md`

对应提交：

- `d7e5b3a Add review command for finish-pack candidates`

然后补了低风险 promote 入口：

- `ieda tools promote-finish-candidates --session-id <id>`

当前不会直接污染正式数据，而是把审核后的候选内容放进：

- `data/pending/cases/`
- `data/pending/logs/`
- `data/pending/benchmarks/`
- `data/pending/promotion_records/`

并追加：

- `data/pending/benchmarks/pending_benchmark_candidates.jsonl`

对应提交：

- `6e60450 Add promotion command for finish-pack candidates`

为了避免候选区和正式区混淆，又补了边界说明：

- [data/pending/README.md](D:\桌面\Industrial%20Embedded%20Dev%20Agent\data\pending\README.md)

对应提交：

- `c258a0b Document pending dataset promotion area`

最后再补上 merge 计划入口：

- `ieda tools plan-pending-merge`

会生成：

- `data/pending/merge_plan/merge_plan.json`
- `data/pending/merge_plan/merge_plan.md`

对应提交：

- `9b805d9 Add merge-plan command for pending dataset`

到这里，这条链已经形成了真正的工程闭环：

1. `finish-real-bench`
2. `review-finish-candidates`
3. `promote-finish-candidates`
4. `plan-pending-merge`

这意味着一次 bench 的结果已经可以从“临时观察”继续推进到：

- 候选 case
- 候选日志样本
- 候选 benchmark
- 待审核候选区
- 待合并计划

---

## 七、今天的验证结果

今天实际跑过或重复验证过的关键命令包括：

- `python -m pytest tests\\test_bench_pack_render.py -q`
- `python -m industrial_embedded_dev_agent check`
- `python -m industrial_embedded_dev_agent check --include-offline --include-rag --rag-type tool_safety`
- `python -m industrial_embedded_dev_agent tools prep-real-bench --session-id bench-am-04 --label "Morning bench v4"`
- `python -m industrial_embedded_dev_agent tools kickoff-real-bench "reports/real_bench_prep/bench-am-04/plan_seed.json" --render-all`
- `python -m industrial_embedded_dev_agent tools finish-real-bench --session-id bench-am-05`
- `python -m industrial_embedded_dev_agent tools review-finish-candidates --session-id bench-am-05`
- `python -m industrial_embedded_dev_agent tools promote-finish-candidates --session-id bench-am-05`
- `python -m industrial_embedded_dev_agent tools plan-pending-merge`

今天这轮推进后的稳定状态是：

- `pytest = 20 passed`
- `rules = 28/28`
- `tool_safety = 5/5`
- `offline_stub_samples = 3/3`

说明当前这套 bench 工具链、离线演练链和数据沉淀链，在本地已经形成比较稳定的工程基线。

---

## 八、当前阶段判断

今天更重要的成果，不是某一个单点命令，而是项目已经从“能做一个工具 demo”继续推进到了“有 bench 工具链、有离线回归、有真机准备、有数据沉淀闭环”的阶段。

当前已经基本具备：

1. 可控工具执行边界
2. `stub / real` 模式切换
3. bench 证据采集、渲染、比较、汇总、索引
4. 固定离线样例与离线自动回归
5. 本地检查与 GitHub Actions CI
6. 真机准备包与 kickoff 入口
7. finish、review、promote、merge plan 数据沉淀链

当前唯一还没有完成的现实验证，仍然是：

- 真实硬件还不在手边
- 尚未做回工位后的第一轮真实只读验证

不过从工程准备角度看，今天已经把“回工位后第一轮 bench 如何启动、如何留证、如何复盘、如何把结果沉淀回项目”这件事，准备得很完整了。

---

## 九、下一步建议

下一阶段最自然的方向有两个：

1. 回工位后切到 `real` 模式，基于准备包直接做首轮只读 bench 验证
2. 在未接真实硬件前，继续补“formal merge assistant”，把 pending 区候选进一步推进到正式数据并入辅助工具

如果先从工程价值和项目成长性考虑，下一步值得优先做的是：

- 基于 `data/pending/` 做正式 merge assistant，但第一版仍然只生成 merge 建议或 patch 建议，不直接改正式数据

如果从业务验证优先级考虑，下一步则是：

- 回工位后按 `prep-real-bench -> kickoff-real-bench --render-all -> finish-real-bench` 跑第一轮真实只读验证

今天这轮真正建立起来的是一个更像工业项目的节奏：

- 先准备
- 再采证
- 再复盘
- 再沉淀
- 最后把现场经验回流到 Agent 本身
